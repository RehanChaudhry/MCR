enum RelationFilterType {
  BLOCKED = "blocked",
  DISMISSED = "dismissed",
  MATCHES = "matches",
  FRIENDS = "friends",
  CANCEL = "cancel",
  CONNECT_REQUEST = "connect_requests",
  ROOMMATE = "roommates"
}

export default RelationFilterType;
