import React, { FC } from "react";
import { SignInView } from "ui/screens/home/signin/SignInView";
type Props = {};
const SignInController: FC<Props> = () => {
  return <SignInView />;
};

export default SignInController;
