import React, { FC } from "react";
import { AppInputFieldView } from "ui/screens/demo/app_input_field/AppInputFieldView";

type Props = {};
const AppInputFieldController: FC<Props> = () => {
  return <AppInputFieldView />;
};

export default AppInputFieldController;
