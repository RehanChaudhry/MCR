import React, { FC } from "react";
import { AccordionDemoView } from "ui/screens/demo/accordion/AccordionDemoView";

type Props = {};

const AccordionController: FC<Props> = () => {
  return <AccordionDemoView />;
};

export default AccordionController;
