import React, { FC } from "react";
import DemoFormView from "./DemoFormView";

type Props = {};

const DemoFormController: FC<Props> = () => {
  return <DemoFormView />;
};

export default DemoFormController;
