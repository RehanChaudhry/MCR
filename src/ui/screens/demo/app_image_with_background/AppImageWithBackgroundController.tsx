import React, { FC } from "react";
import { AppImageWithBackgroundView } from "ui/screens/demo/app_image_with_background/AppImageWithBackgroundDemoView";

type Props = {};

const AppImageWithBackgroundController: FC<Props> = () => {
  return <AppImageWithBackgroundView />;
};

export default AppImageWithBackgroundController;
