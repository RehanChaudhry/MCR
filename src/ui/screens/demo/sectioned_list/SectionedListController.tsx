import React, { FC } from "react";
import { SectionedListView } from "./SectionedListView";

type Props = {};

const SectionedListController: FC<Props> = () => {
  return <SectionedListView />;
};

export default SectionedListController;
